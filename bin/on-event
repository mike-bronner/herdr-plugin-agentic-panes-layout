#!/bin/sh
set -eu

case "${HERDR_PLUGIN_EVENT:-}" in
    worktree.created|worktree.opened) ;;
    *) exit 0 ;;
esac

PLUGIN_ROOT="${HERDR_PLUGIN_ROOT:-$(CDPATH='' cd -- "$(dirname -- "$0")/.." && pwd)}"

exec "$PLUGIN_ROOT/bin/agent-layout" --from-event
